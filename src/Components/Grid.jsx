import React from 'react';
import styles from './CSS/grid.module.css';

const Grid= (props) => {
    return (
        <div className={styles.container}>
            
        </div>
    );
} 

export default Grid;